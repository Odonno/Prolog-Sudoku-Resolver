<UserControl x:Class="PrologSudoku.UI.UserControls.ValueUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid MouseLeftButtonDown="OnMouseLeftButtonDown">
        <Border Grid.Column="1" BorderThickness="1" BorderBrush="Black">
            <TextBlock Foreground="Black" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                   Text="{Binding Path=Value, Converter={StaticResource SquareValueToStringConverter}}" />
        </Border>
    </Grid>
</UserControl>
