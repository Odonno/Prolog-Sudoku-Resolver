<UserControl x:Class="PrologSudoku.UI.UserControls.GenerateValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:converters="clr-namespace:PrologSudoku.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

        <UserControl.Resources>
            <converters:SquareValueToStringConverter x:Key="BooleanToFillConverter" />
        </UserControl.Resources>

        <Rectangle x:Name="rectangle" Stroke="Black" StrokeThickness="0.25" Fill="{Binding ElementName=RectangleValueControl, Path=Value.IsSelected}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseLeftButtonDown">
                    <command:EventToCommand Command="{Binding Path=SetValueOnRectangle}" CommandParameter="{Binding ElementName=RectangleValueControl, Path=Value}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Rectangle>

</UserControl>
